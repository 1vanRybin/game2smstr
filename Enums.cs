namespace MazeEscape;

public enum Mode
{
    Menu,
    Game,
    Rules,
    GameOver,
    Exit
}

public enum MenuState
{
    StartGame,
    Rules,
    Exit
}

public enum Skill
{
    DoubleMove,
    Invisibility
}
