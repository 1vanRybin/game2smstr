namespace MazeEscape;

public enum Mode
{
    Menu,
    Game,
    Rules,
    GameOver,
    WinGame,
    Exit
}

public enum MenuState
{
    StartGame,
    Rules,
    Exit
}

public enum Skill
{
    DoubleMove,
    Invisibility
}
